name: CI/CD Planning Poker Full Stack

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: write
  pages: write
  id-token: write
  pull-requests: write

jobs:
  # Job 1 : Tests Backend
  backend-tests:
    name: Backend - Tests et Documentation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
    
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
    
      - name: Installation des dépendances
        run: npm ci
    
      - name: Vérifier le serveur
        run: |
          echo "✅ Backend prêt"
          node --version
          npm --version

  # Job 2 : Tests Frontend
  frontend-tests:
    name: Frontend - Build et Validation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
    
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: package-lock.json
    
      - name: Installation des dépendances
        run: npm ci
    
      - name: Build de production
        run: npm run build
        
      - name: Vérifier les fichiers générés
        run: |
          echo "✅ Build terminé avec succès"
          ls -la dist/

  # Job 3 : Validation Build
  validate-build:
    name: Validation Build Full Stack
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend-tests]
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
    
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
    
      - name: Build Backend
        run: |
          cd backend
          npm ci
          echo "✅ Backend prêt"
    
      - name: Build Frontend
        run: |
          npm ci
          npm run build
          echo "✅ Frontend buildé"

  # Job 4 : Résumé CI/CD
  summary:
    name: Résumé CI/CD
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend-tests, validate-build]
    if: always()
    
    steps:
      - name: Générer rapport de synthèse
        run: |
          echo "# 📊 Rapport CI/CD - Planning Poker" >> $GITHUB_STEP_SUMMARY
          echo "**Date** : $(date)" >> $GITHUB_STEP_SUMMARY
          echo "**Branche** : ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 📋 Résumé des jobs" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### 🟢 Backend Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Dépendances installées" >> $GITHUB_STEP_SUMMARY
          echo "- Serveur validé" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### 🟢 Frontend Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Build de production validé" >> $GITHUB_STEP_SUMMARY
          echo "- Fichiers générés correctement" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### 🟢 Validation Build" >> $GITHUB_STEP_SUMMARY
          echo "- Build backend et frontend vérifiés" >> $GITHUB_STEP_SUMMARY
          echo "- Application prête pour le déploiement" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 📈 Étapes suivantes" >> $GITHUB_STEP_SUMMARY
          echo "1. Vérifier les logs pour détecter d'éventuels warnings" >> $GITHUB_STEP_SUMMARY
          echo "2. Déployer l'application si tous les tests passent" >> $GITHUB_STEP_SUMMARY